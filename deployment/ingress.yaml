apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: user04-svc
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  tls:
  - secretName: tls-secret
  rules:
  - host: "user04.169.56.100.104.nip.io"
    http:
      paths:
      - path: /user04
        pathType: Prefix
        backend:
          service:
            name: sample
            port:
              number: 8080
